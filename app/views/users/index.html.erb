<%= flash[:success] %>
<div class="container">
    <div class="row mb-5">
        <div class="col-lg-12">
          <p>
            <button type="button" class="btn btn-primary mt-4" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
            <h5 class="mt-2">検索する</h5>
            </button>
          </p>
          <div class="collapse" id="collapseExample">
            <div class="card card-body">
              <div> 
                <%= form_tag(users_path, method:"get") do %>
                <%= label_tag :username, "名前:" %><%= text_field_tag :username, params[:username], :class => "ml-2" %>
                <div class="form-inline my-2">
                  <%= label_tag :height, "身長:" %>
                  <%= number_field_tag :height1, params[:height1], :class => "mx-2"%>以上 ~
                  <%= number_field_tag :height2, params[:height2], :class => "mx-2"%>以下
                </div>
                <div class="form-inline">
                  <%= label_tag :age, "年齢:" %>
                  <%= number_field_tag :age1, params[:age1], :class => "mx-2"%>以上 ~
                  <%= number_field_tag :age2, params[:age2], :class => "mx-2"%>以下
                </div>
                <div class="form-inline my-2">
                  <%= label_tag :occupation, "職種:" %>
                  <%= select_tag :occupation, options_for_select(Profile.occupations), include_blank: true, :class => "ml-2" %>
                  <%= submit_tag "検索", :class => "ml-4" %>
                </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
        <div class="col-lg-12">
          <h2 class="my-4 ml-2">Users</h2>
        </div>
        <% @profiles.each do |profile| %>
        <div class="col-lg-4 col-sm-6 text-center mb-4">
          <%= link_to(user_path(profile.user_id), style:'text-decoration: none') do %>
            <% if profile.image? %>
              <%= image_tag profile.image.thumb.url, :class => "rounded-circle img-fluid d-block mx-auto mb-2" %>
            <% else %>  
              <img class="rounded-circle img-fluid d-block mx-auto mb-2" src="../img/noimage.jpg" alt="avatar">
            <% end %>
            <h3 class="text-dark"><%= profile.username %> <small><%= profile.age %></small></h3>
            <% if profile.singleword? %>
              <p class="text-dark"><%= truncate(profile.singleword, length: 17) %></p>
            <% end %>
          <% end %>  
        </div>    
        <% end %>    
    </div>
</div>    